import React from 'react';
import Loader from '@bit/feathericons.react-feather.loader';
import { getRandomColor } from '@bit/joshk.jotils.get-random-color';

export default <Loader size='50' color={getRandomColor()} />;
